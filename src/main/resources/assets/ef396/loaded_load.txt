# Load the following `Loaded` classes.
# Comments by # as first line character.
# Main module `Loader` load all classes in the list
# and so instantiates the `static` fields.
# BASIC ExactFeather factorized content.
Loaded
# Add your loader class name here, and place it
# in the same directory as `Loader`.
# You can use relative class paths such as `loaded.Class`.
# You are advised to extend class `loaded.BlankLoaded`.
loaded.BlankLoaded
